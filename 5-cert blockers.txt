> any other cert blockers?

● From what we verified today, three remain:

  HIGH — Gate 2 is effectively disabled (V-003)
  No phasor coherence estimator exists in any repo. core.py now documents the pass-through, but the gate is still a
  no-op. Every cert skips coherence checking entirely. This is the most load-bearing gap — Gate 2 is supposed to be the
  phasor coherence firewall before kappa is even evaluated.

  HIGH — certifier.py field rename may break callers
  We renamed RSCTCertificate.kappa → svd_condition_score in swarm-it-api. Any caller that reads .kappa off that
  dataclass will get AttributeError. Worth grepping before next deploy:
  grep -rn "\.kappa" ~/github/swarm-it-api --include="*.py" | grep -v "__pycache__" | grep -v
  "kappa_gate\|kappa_H\|kappa_L\|kappa_A\|kappa_interface\|kappa_req"

  MEDIUM — omega still proxied (V-001)
  Production omega = 1 - S_sup (proxy). GeometryCompatibilityChecker exists but is unwired. Claim 5 ("reliability
  coefficient from latent variance, without labeled OOD data") is indefensible until it's wired. Gate 3's Oobleck
  threshold doesn't use omega, so it doesn't affect enforcement correctness today — but the patent gap is real.

  Everything else from the session (decision placement, kappa_A in min, sigma formula, gate 3 decision) is resolved.