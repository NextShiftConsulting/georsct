Review the s035 experiment pipeline code for a NeurIPS geo-cert paper. Focus on:

1. **Fold generation fix** (generate_folds.py): A new block-merging and adaptive n_folds mechanism was added to eliminate catastrophic fold size imbalance (Houston had 1-ZCTA folds, Riverside had 1-ZCTA folds). Check correctness, edge cases, determinism, backward compatibility.

2. **Statistical validity**: The money table (compute_uplift_table.py) uses Wilcoxon signed-rank paired tests across folds. With variable n_folds per scenario (Houston=4, Riverside=3, others=5), check whether pooled tests are valid, whether fold-size weighting is needed, and whether small-fold R2 estimates are meaningful.

3. **Pipeline dependencies**: The experiment runs R0->R1->R2 training, then certificates, diagnostics, money table, DGM routing, R3 pipeline, VLM assessment, and FAST validation. Check for missing dependencies, stale artifacts, format mismatches.

4. **Data gaps**: New Orleans has no histgbdt training (ridge only). NYC is missing histgbdt at R2. Riverside HWM classification is 100% NaN. Check whether the decisions to launch/exclude are sound.

5. **ADR-034 compliance**: certificate.py was renamed S->S_sup, kappa_gate->kappa_compat, decision->public_decision. Check backward compatibility of deserialization.

6. **Contract completeness**: EXPERIMENT_CONTRACT.yaml was updated. Check for missing phases, duplicate keys, incorrect dependencies.

Cite file:line for each finding. Do not flag style or naming issues.
